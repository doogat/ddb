# CLAUDE.md

@AGENTS.md

## (Optional) Claude-only notes

- If instructions conflict, follow AGENTS.md.
- Run e2e tests (`cargo test -p ddb-e2e`) after any task that deletes or replaces existing code paths. Skip after purely additive tasks.
