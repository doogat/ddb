name: Full Validation

on:
  workflow_call:
  workflow_dispatch:

permissions:
  statuses: write

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

jobs:
  linux-clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

  linux-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config
      - name: Configure git identity
        run: |
          git config --global user.email "ci@ddb.dev"
          git config --global user.name "Doogat DB CI"
      - name: Sync integration tests
        run: cargo test -p ddb-core --test sync_test
      - name: Property tests
        env:
          PROPTEST_CASES: 50
        run: cargo test -p ddb-core --test property_tests

  linux-thresholds:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config
      - name: Configure git identity
        run: |
          git config --global user.email "ci@ddb.dev"
          git config --global user.name "Doogat DB CI"
      - name: Query thresholds
        run: cargo test --release -p ddb-core --test query_thresholds nfr01_
      - name: Growth thresholds
        run: cargo test --release -p ddb-core --test growth_thresholds nfr02_
      - name: Sync thresholds
        run: cargo test --release -p ddb-core --test sync_thresholds nfr03_

  linux-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config
      - name: Configure git identity
        run: |
          git config --global user.email "ci@ddb.dev"
          git config --global user.name "Doogat DB CI"
      - name: Build CLI binary
        run: cargo build -p ddb-cli --bin ddb
      - name: Integration test
        env:
          DDB_BIN: ${{ github.workspace }}/target/debug/ddb
        run: ./tests/integration.sh

  linux-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config
      - name: Configure git identity
        run: |
          git config --global user.email "ci@ddb.dev"
          git config --global user.name "Doogat DB CI"
      - name: Build CLI binary
        run: cargo build -p ddb-cli --bin ddb
      - name: Full e2e tests
        env:
          DDB_BIN: ${{ github.workspace }}/target/debug/ddb
        run: cargo test -p ddb-e2e

  macos-full:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Configure git identity
        run: |
          git config --global user.email "ci@ddb.dev"
          git config --global user.name "Doogat DB CI"
      - name: Build CLI binary
        run: cargo build -p ddb-cli --bin ddb
      - name: Full cargo test suite
        env:
          PROPTEST_CASES: 50
        run: cargo test-full
      - name: Integration test
        env:
          DDB_BIN: ${{ github.workspace }}/target/debug/ddb
        run: ./tests/integration.sh

  windows-full:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Configure git identity
        run: |
          git config --global user.email "ci@ddb.dev"
          git config --global user.name "Doogat DB CI"
      - name: Build CLI binary
        run: cargo build -p ddb-cli --bin ddb
      - name: Full cargo test suite
        env:
          PROPTEST_CASES: 50
        run: cargo test-full
      - name: Integration test
        timeout-minutes: 15
        env:
          DDB_BIN: ${{ github.workspace }}\target\debug\ddb.exe
        run: pwsh -File tests/integration.ps1

  mark-validated:
    needs:
      - linux-clippy
      - linux-core
      - linux-smoke
      - linux-e2e
      - macos-full
      - windows-full
    runs-on: ubuntu-latest
    steps:
      - name: Post full-validation/passed commit status
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api repos/${{ github.repository }}/statuses/${{ github.sha }} \
            -f state=success \
            -f context=full-validation/passed \
            -f description="All full-validation jobs passed"
